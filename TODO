FOR TODAY: fix all memory leaks

check correctness of set_env_val function
check correctness of buitlin cd implementation
implement free_command function

add better env parsing, cause some values can contain '=' sign, ans split will behave incorrect
check export builitin func & implement valid of args

add check if last symbol in STDOUT is new line or not (to avoid shell prompt on one line with prev output)

check bin permissions -> done???
check if S_ISREG macros forbidden?
find info why do i need global variable for signals

Hakob:
make func that will free cmd
